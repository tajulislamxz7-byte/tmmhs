git config --global --add safe.directory 'E:/School Website - Copy'
git config --global user.email "admin@tiarkhali-mmhs.edu.bd"
git config --global user.name "Tajul"
git add -A
git commit -m "Fix: notice/event edit, principal edit form, emoji to SVG icons, settings layout, compact edit modal, school photo upload, about page data fetch"
git push origin main
